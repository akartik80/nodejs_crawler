"use strict";

const config = {
  application: {
    parallelRequestLimit: 5,
    matcherRegex: /medium\.com/g
  },

  winston: {
    level: 'debug'
  },

  redis: {
    host: null,
    port: null,
    db: null,
    password: null
  }
};

module.exports = config;
